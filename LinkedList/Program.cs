/*
public class Program 
{
    static void Main(string[] args) 
    {
        LinkedList<int> list = new();

        list.AddElementToList(7);
        list.AddElementToList(1);
        list.AddElementToList(8);
        list.AddElementToList(2);
        list.AddElementToList(8);
        list.AddElementToList(3);

        // 7 1 8 2 8 3
        foreach (var element in list)
            Console.WriteLine(element);
        Console.WriteLine();

        // 7 1 8 2 8 
        list.RemoveFirstFromList(3);

        foreach (var element in list)
            Console.WriteLine(element);
        Console.WriteLine();

        // 7 1 2 8 
        list.RemoveFirstFromList(8);

        foreach (var element in list)
            Console.WriteLine(element);
        Console.WriteLine();

        // 1 2 8
        list.RemoveFirst();

        foreach (var element in list)
            Console.WriteLine(element);
        Console.WriteLine();

        // 1 2
        list.RemoveLast();

        foreach (var element in list)
            Console.WriteLine(element);
        Console.WriteLine();
        
        // 1 
        list.RemoveLast();

        foreach (var element in list)
            Console.WriteLine(element);
        Console.WriteLine();    
        
        //
        list.RemoveLast();

        foreach (var element in list)
            Console.WriteLine(element);
        Console.WriteLine();

        //
        list.RemoveLast();

        foreach (var element in list)
            Console.WriteLine(element);
        Console.WriteLine("end");
    }
}
*/
